// inicializa classlib na pasta CQRS-ES
dotnet new classlib -o CQRS.Core

// crie a solution na pasta SM-Post
dotnet new sln

// cria webapi na pasta Post.Cmd
dotnet new webapi -o Post.Cmd.Api

// cria ainda na pasta Post.Cmd o template class library
dotnet new classlib -o Post.Cmd.Domain

// cria ainda na mesma pasta (Post.Cmd) a class library Infrastructure
dotnet new classlib -o Post.Cmd.Infrastructure